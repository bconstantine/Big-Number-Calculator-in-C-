

#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <math.h>
#include "BigNumber.h"

using namespace std;

BigNumber add(BigNumber numA, BigNumber numB);
BigNumber subtract(BigNumber numA, BigNumber numB);
BigNumber multiply(BigNumber numA, BigNumber numB);
BigNumber divide(BigNumber numA, BigNumber numB);
BigNumber power(BigNumber numA, BigNumber numB);
BigNumber factorial(BigNumber num);
